# [2026-01-16] Meeting Notes Summary Stabilization & Error Debugging

## 1. 📝 요약 (Summary)
회의록 요약 및 자동 일정 등록 기능의 실사용 안정성을 확보하기 위해 발생한 다수의 오류를 디버깅하고 해결했습니다. 특히 다중 턴 인터랙션에서의 상태 보존과 에이전트의 출력 품질을 개선했습니다.

## 2. 🚀 주요 변경 사항 (Key Changes)

### 🐞 해결된 주요 오류 (Error Resolution)
- **UnboundLocalError Fix**: `executor_node`에서 디버깅 중 누락된 변수 정의로 인한 중단 해결.
- **ValueError (f-string) Fix**: 시스템 프롬프트 내 JSON 예시의 `{}` 이스케이프 누락으로 인한 서버 Crash 해결 (증상: "아무것도 안 나옴").
- **Terminal Noise Analysis**: `CancelledError`, `KeyboardInterrupt` 등의 터미널 에러는 서버 자동 재동작(`--reload`) 시 발생하는 정상 신호임을 확인.
- **Response Timeout Fix**: 모델 응답이 3분 이상 지연될 경우 자동으로 모델을 초기화(Unload)하고 1회 재시도하는 로직 구현.

### 🏗️ 구조 및 기능 개편 (Architectural Improvements)
- **Split-Task Flow**: 분석(Analysis)과 등록(Registration) 단계를 분리하여 안정성 확보.
- **Context Persistence**: `raw_meeting_notes` 필드를 통한 물리적 원문 보존.
- **Default Calendar Binding**: 회의록 관련 일정 등록 시 자동으로 **[WS] Inc.** 캘린더에 할당되도록 로직 고정.

## 3. 🧠 기술적 맥락 (Context for AI)
- **Timeout Rationale**: Ollama 등 로컬 LLM 환경에서 특정 답변 생성 시 CPU/GPU 점유로 인해 무한 대기나 스와핑이 발생할 수 있음. 180초 타임아웃과 `unload_model` 호출을 통해 자원을 강제 회수하고 깨끗한 상태에서 재시도하도록 설계함.
- **Terminal Signals**: `uvicorn` 리로더가 프로세스를 죽일 때 발생하는 `CancelledError`는 예외 처리가 아닌 정상 종료 과정임.

## 4. ✅ 향후 개발 계획 (Roadmap Update)
- **Phase 3: Robustness & Context Recovery (진행 중)**
  - [x] 3분 타임아웃 및 모델 재초기화 로직 (`main.py`)
  - [ ] 일반적인 상황에서의 히스토리 기반 컨텍스트 복구 로직 강화
- **Phase 4: Smart Memory & Profile (예정)**
  - [ ] 장기 기억(Long-term pattern) 분석 및 프로필 자동 업데이트
  - [ ] UI/UX 레이아웃 최적화 (Flutter 클라이언트)
